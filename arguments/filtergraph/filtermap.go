package filtergraph

/*
import "github.com/DanielEnricoBotta94/go-ffmpeg/ffmpeg"

type FilterGraph struct {
	FilterChain []string
	MapChain []string
}

type Map ffmpeg.Ffmpeg

func (fg *FilterGraph) Map(output string) *ffmpeg.Builder {

}

func (fm *FilterBuilder) Map(output string) *Output {
	fm.MapChain = append(fm.Arguments.FilterGraph.MapChain, output)
	return fm
}

func (fm *Builder) Output(output string) *ffmpeg.Builder {
	fm.
}

func (fm *Builder) Build() []string {
	result := []string{"-filter_complex"}
	for _, value := range *fm.FilterChain {
		result = append(result, value)
	}

	for _, value := range fm.MapChain {
		result = append(result, "-map")
		result = append(result, value)
	}

	return result
}
*/
